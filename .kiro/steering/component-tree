├── /kiosk (Kiosk Workspace)
│   ├── KioskLogin.jsx (Numeric keypad, OTP input, auto-timeout wrapper)
│   ├── KioskTriage.jsx (Voice waveform visualizer, QR canvas, 45s auto-reset timer)
│   └── VoiceAssistant.jsx (Audio recording, STT/TTS playback streaming)
├── /mobile (Mobile PWA Workspace)
│   ├── DocumentUpload.jsx (Camera capture, client-side compression, retry counter)
│   └── StatusPoller.jsx (5s interval polling, cross-sell offer display cards)
└── /teller (Dashboard Workspace)
    ├── QueueList.jsx (Sidebar ticket list, AML warning badges, status filters)
    ├── SecureImageDisplay.jsx (Signed URL fetcher, pan/zoom image inspection viewport)
    └── ApprovalControls.jsx (Action buttons, rejection reason dropdown, AML escalation trigger)